<screen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/xml-screen-3.xsd">
    <parameter name="workEffortId"/>
    <!-- <parameter name="selectedWorkEffortId"/>
    <transition name="updateTask"><service-call name="update#mantle.work.effort.WorkEffort" in-map="context" out-map="context"/>
        <default-response url="../../MyTasks?workEffortId=${workEffortId}"/></transition>
        -->
    <transition name="updateTask"><service-call name="update#mantle.work.effort.WorkEffort" in-map="context" out-map="context"/>
        <default-response url=".."/></transition>
    <transition name="addComment"><service-call name="mantle.work.TaskServices.add#TaskComment"/>
        <default-response url="../../MyTasks?selectedWorkEffortId=${workEffortId}"/></transition>
    <transition name="deleteComment"><service-call name="mantle.work.TaskServices.delete#TaskComment"/>
        <default-response url="../../MyTasks?selectedWorkEffortId=${workEffortId}"/></transition>
    <subscreens>
        <subscreens-item name="TaskCommentReply" location="component://SimpleScreens/screen/SimpleScreens/Task/TaskSummary/TaskCommentReply.xml"/>
        <subscreens-item name="TaskCommentUpdate" location="component://SimpleScreens/screen/SimpleScreens/Task/TaskSummary/TaskCommentUpdate.xml"/>
    </subscreens>
    <actions>
        <entity-find-one entity-name="mantle.work.effort.WorkEffort" value-field="workEffort"/>
        <!--
        <entity-find-one entity-name="mantle.work.effort.WorkEffort" value-field="workEffort">
            <field-map field-name="workEffortId" from="selectedWorkEffortId"/>
        </entity-find-one>
        -->
        <!-- To support status change -->
        <set field="statusId" from="workEffort?.statusId"/>
        <set field="workEffortId" from="workEffort?.workEffortId"/>
        <set field="statusChangeTransition" value="updateTask"/>

        <!-- Comments -->
        <entity-find entity-name="mantle.work.effort.WorkEffortCommEventDetail" list="commEventDetailList">
            <econdition field-name="workEffortId"/><order-by field-name="entryDate"/></entity-find>
    </actions>

    <widgets>
        <label text="Task: ${workEffortId}"/>
        <container-box><box-header/>
            <!--<box-header title="${workEffort?.workEffortName ?: 'Please Select a Task'}"/>-->
            <box-toolbar>
                <section-include name="StatusChangeSection" location="component://SimpleScreens/template/basic/StatusWidgets.xml"/>
            </box-toolbar>
            <box-body height="75vh">
                <form-single name="TaskDetailForm" transition="updateTask" map="workEffort">
                    <!--
                    <field name="selectedWorkEffortId"><default-field><hidden/></default-field></field>
                    <field name="workEffortId" from="selectedWorkEffortId"><default-field><hidden/></default-field></field>
                    -->
                    <field name="workEffortId"><default-field><hidden/></default-field></field>
                    <field name="workEffortName"><default-field title="Name"><text-line size="40"/></default-field></field>
                    <!-- TODO: get list of users in system to assign tasks. This example is from FindTask in HM. not exactly what I want.
                    <field name="partyId"><default-field title="Assigned To">
                        <drop-down allow-multiple="true">
                            <dynamic-options transition="getProjectParties" value-field="partyId" label-field="name"
                                             depends-optional="true" server-search="true" min-length="0" parameter-map="[addOptional:'false']">
                                <depends-on field="rootWorkEffortId"/></dynamic-options>
                        </drop-down>
                    </default-field></field>
                    -->
                    <!-- Maybe a better example. Need to default to current assigned user and be able to change from list of users in system.
                    <field name="assignToPartyId"><default-field title="Assign To">
                        <drop-down no-current-selected-key="${ec.user.userAccount.partyId}" allow-empty="true">
                            <dynamic-options transition="getProjectParties" value-field="partyId" label-field="name"
                                    depends-optional="true" server-search="true" min-length="0" parameter-map="[addOptional:'false']">
                                <depends-on field="rootWorkEffortId"/></dynamic-options>
                        </drop-down>
                    </default-field></field>
                    -->
                    <field name="estimatedCompletionDate"><default-field title="Due Date"><date-time type="date" format="yyyy-MM-dd"/></default-field></field>
                    <field name="description"><default-field title="Description"><text-area rows="10" cols="60"/></default-field></field>
                    <field name="submitButton"><default-field title="Update"><submit/></default-field></field>
                    <!-- Not necessary unless more fields are added in the future and need to be aligned in the same row.
                    <field-layout>
                        <field-row-big><field-ref name="workEffortName"/></field-row-big>
                        <field-row-big><field-ref name="estimatedCompletionDate"/></field-row-big>
                        <field-row-big><field-ref name="description"/></field-row-big>
                        <field-row-big><field-ref name="submitButton"/></field-row-big>
                    </field-layout>
                    -->
                </form-single>

        <container-box><box-header title="Comments"/>
            <box-toolbar>
                <container-dialog id="AddCommentDialog" button-text="Add Comment">
                    <form-single name="AddCommentForm" transition="addComment">
                        <field name="workEffortId"><default-field><hidden/></default-field></field>
                        <field name="body"><default-field title="Comment"><text-area rows="10"/></default-field></field>
                        <field name="submitButton"><default-field title="Add"><submit/></default-field></field>
                    </form-single>
                </container-dialog>
            </box-toolbar>
            <box-body>
                <section-iterate name="CommentList" list="commEventDetailList" entry="commEventDetail">
                    <actions>
                        <set field="uniqueExtension" from="commEventDetail_index"/>
                        <entity-find-one entity-name="mantle.party.Person" value-field="fromPerson" cache="true">
                            <field-map field-name="partyId" from="commEventDetail.fromPartyId"/>
                        </entity-find-one>
                    </actions>
                    <widgets><container-box>
                        <box-header title="${fromPerson ? fromPerson.firstName + ' ' + fromPerson?.lastName : 'Unknown'} ${ec.l10n.format(commEventDetail.entryDate, 'yyyy-MM-dd HH:mm')}"/>
                        <box-toolbar>
                            <dynamic-dialog id="EditCommentContainer${uniqueExtension}" button-text="Edit" transition="TaskCommentUpdate" condition="commEventDetail.fromPartyId == ec.user.userAccount.partyId">
                                <parameter name="communicationEventId" from="commEventDetail.communicationEventId"/></dynamic-dialog>
                            <link text="Delete" url="deleteComment" parameter-map="[workEffortId:workEffortId,
                                        communicationEventId:commEventDetail.communicationEventId]" condition="commEventDetail.fromPartyId == ec.user.userAccount.partyId"
                                  confirmation="Delete comment ${commEventDetail.subject}?"/>
                        </box-toolbar>
                        <box-body>
                            <label text="${commEventDetail.body}" encode="false" condition="commEventDetail?.body"/>
                        </box-body>
                    </container-box>
                </widgets></section-iterate>
            </box-body></container-box></box-body></container-box>
    </widgets>
</screen>